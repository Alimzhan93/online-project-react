import React from 'react';
import { SelectedPage } from '@/types/TSelectedPage';

type Props = {
    setSelectedPage: (value: SelectedPage) => void;
};

const BenefitItem = (props: Props) => {
    return <div>BenefitItem</div>;
};

export default BenefitItem;
